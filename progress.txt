# Progress log

- 2026-01-09: Implemented core message-sending behavior via a minimal `ChatStore` (in-memory) with a Vitest unit test.
  - Added `package.json` + `tsconfig.json` with `pnpm typecheck` and `pnpm test` scripts.
  - Note: this environment denied running `pnpm`/`node` and `git add/commit` (permission prompt couldn’t be shown), so checks + commit must be done manually.
  - Next: implement persistence after refresh/reopen (likely via a storage abstraction) and then real-time delivery.

- 2026-01-19: Implemented `/raid refresh` command for raid roster and embed management.
  - Added refresh subcommand to `/raid` command with proper permissions checking.
  - Refreshes raid roster by scanning current eligible members with raid roles.
  - Adds new members who gained raid role after raid creation.
  - Removes members who lost raid role.
  - Updates embed message with latest design and roster changes.
  - Provides detailed feedback showing count of added/removed members.
  - Marks three PRD features as passing: refresh adds members, refresh removes ineligible, refresh updates embed.
  - Build passes with `npm run build`.
  - Next: implement optional roles parameter for `/raid create` command.

- 2026-01-19: Implemented optional roles parameter for `/raid create` command.
  - Added optional 'roles' string parameter to `/raid create` subcommand.
  - When roles parameter is provided, uses custom roles instead of configured raid roles.
  - Custom roles are comma-separated role names or IDs (e.g., "Role1,Role2").
  - When roles parameter is omitted, falls back to configured raid roles (existing behavior).
  - Updated error message to indicate which roles were used when no eligible members found.
  - Marks two PRD features as passing: custom roles parameter works, and default behavior unchanged.
  - Build passes with `npm run build`.
  - Next: work on embed layout or documentation features.

- 2026-01-19: Verified and documented existing 3-column raid embed layout implementation.
  - Confirmed raid embed displays attendance in 3-column layout: Tank | Healer | DPS.
  - Tank column shows on left with 🛡️ icon and count.
  - Healer column shows in middle with 💚 icon and count.
  - DPS column shows on right with ⚔️ icon, combining Melee + Ranged into single count.
  - Each column shows role count and sorted player names with spec symbols.
  - Late players display in dedicated ⏰ section below main columns.
  - Opted-out players display in dedicated ❌ section below main columns.
  - Players with no class set display in ❓ section below main columns.
  - Marks four PRD features as passing: 3-column layout, late section, opted-out section, no-class section.
  - All functional features now complete (8/8 passing).
  - Build passes with `npm run build`.
  - Next: work on documentation features.

